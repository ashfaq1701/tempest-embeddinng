# Runtime dependencies. The codebase is being rebuilt from first
# principles (see CLAUDE.md); these four packages are the stable
# TGB-interfacing + numerical layer that survives the rewrite.
#
# `torch` is intentionally NOT pinned to a specific CUDA wheel here —
# install the matching build for your GPU first from the PyTorch index,
# e.g.
#     pip install torch --index-url https://download.pytorch.org/whl/cu126
# (a CPU-only wheel works too.)

numpy>=1.24,<3
torch>=2.0
py-tgb>=2.0          # imports as `tgb`; PyPI's `tgb` is an unrelated Telegram wrapper
temporal-random-walk>=1.8.2
